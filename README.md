Сервис реализующий умный поиск.

!models - Pydantic модели

!services - файлы под ML модели, пусть будет 1 файл == 1 модель в виде класса,
струкутур класса можно унифицировать, если не впадлу будет создам базовый класс.
!up нанейронил базовый класс, хз, можно от него наследоваться можно забить

!routers - роутеры, пока можно не использовать, но вдруг пригодится, го пока что
все эндпоинты в main.py писать.

- Запуск uvicorn app.main:app --host localhost --port 8000 --reload